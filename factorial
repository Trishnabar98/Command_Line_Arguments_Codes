#include <stdio.h>
#include <stdlib.h>

int main(int argc,char *argv[])
{
    int i,n;
    unsigned long long f=1;
    n=atoi(argv[1]);
    for(i=1;i<n;i++)
    {
        f*=i;
    }
    printf("factorial of %d is:%llu \n",n,f);
    return 0;
}


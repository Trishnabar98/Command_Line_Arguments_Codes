#include <stdio.h>
#include <stdlib.h>

int main(int argc,char *argv[])
{
    int a,b,temp;
    a=atoi(argv[1]);
    b=atoi(argv[2]);
    
    printf("before swap,a=%d,b=%d\n",a,b);
    temp=a;
    a=b;
    b=temp;
    printf("after swap,a=%d,b=%d\n",a,b);

    return 0;
}

